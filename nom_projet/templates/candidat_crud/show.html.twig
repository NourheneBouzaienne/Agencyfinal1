{% extends 'dashboard.html.twig' %}

{% block title %}Candidat{% endblock %}

{% block body %}
    <h1>Candidat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ candidat.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ candidat.nom }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ candidat.status }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ candidat.image }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ candidat.age }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('candidat_crud_index') }}">back to list</a>

    <a href="{{ path('candidat_crud_edit', {'id': candidat.id}) }}">edit</a>

    {{ include('candidat_crud/_delete_form.html.twig') }}
{% endblock %}
