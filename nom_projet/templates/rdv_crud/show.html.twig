{% extends 'base.html.twig' %}

{% block title %}Rdv{% endblock %}

{% block body %}
    <h1>Rdv</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rdv.id }}</td>
            </tr>
            <tr>
                <th>NomCandidat</th>
                <td>{{ rdv.NomCandidat }}</td>
            </tr>
            <tr>
                <th>EmailCandidat</th>
                <td>{{ rdv.EmailCandidat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rdv_crud_index') }}">back to list</a>

    <a href="{{ path('rdv_crud_edit', {'id': rdv.id}) }}">edit</a>

    {{ include('rdv_crud/_delete_form.html.twig') }}
{% endblock %}
