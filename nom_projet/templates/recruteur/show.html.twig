{% extends 'dashboard.html.twig' %}

{% block title %}Recruteur{% endblock %}

{% block body %}
    <h1>Recruteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recruteur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ recruteur.nom }}</td>
            </tr>
            <tr>
                <th>Departement</th>
                <td>{{ recruteur.departement }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ recruteur.image }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-dark" href="{{ path('recruteur_index') }}">back to list</a>

    <a class="btn btn-warning" href="{{ path('recruteur_edit', {'id': recruteur.id}) }}">edit</a>

    {{ include('recruteur/_delete_form.html.twig') }}
{% endblock %}
